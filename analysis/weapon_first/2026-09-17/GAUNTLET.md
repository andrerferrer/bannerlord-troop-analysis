# Weapon-first gauntlet execution record

## Boundary

This task audits the pinned repository weapon evidence and implements a comparator
that refuses unsupported inputs. It does not simulate battles, validate the live
installation, supply missing crafted-weapon statistics, or recalibrate frozen
models. The reviewer was the executing assistant using distinct adversarial passes;
no separate independent reviewer agent is claimed.

Base: `9311f5596aabc04974d5fe4d7113f1088454143f`.
PR: #105. Operator decision date: 2026-09-17; execution crossed into 2026-09-18 UTC.

## Executed loop

| Pass | Actual observation | Correction or result |
|---|---|---|
| Source transport | Local Git network failed DNS resolution | A branch-bounded GitHub Actions job produced a SHA-256-verified source Git bundle; an offline clone enabled full local repository inspection and tests. |
| Inventory review | Shared practice-weapon matches filled the initial 60-row display cap before all target equipment appeared | Target troop rows and item-only rows were selected separately; full scan counts remained explicit. |
| First test pass | 30 scoped tests passed | Added adversarial probes rather than treating the first green pass as sufficient. |
| Red pass | 11 primary-input tests ran; 2 new probes failed | Unknown mount/context and unverified usage/mounted usability could pass the comparator. |
| Fix and green | Tightened explicit context, mount, usage and source requirements | 32/32 scoped tests passed; no skill, template or generic speed fallback. |
| Production recheck | Two full generations matched byte-for-byte | All 13 generated files and their declared hashes validated. |
| Full regression | 432 tests ran: 429 passed and 3 failed | The same 3 tests failed on the detached unchanged base; not a green full-suite claim. |
| Remote execution | Actions run 35299259724 completed successfully | Ran the scoped tests and source-pinned generator, then committed the reproducible outputs. |
| Final workflow | Temporary transport/publication permissions were no longer needed | Replaced the write-enabled job with read-only validation of tests, generated bytes, actual queue, and exact diff scope. |

## Baseline regression exceptions

These failures were reproduced individually on the unchanged base above:

- `test_audit_context_first_candidates.ContextFirstCandidateAuditTests.test_staged_path_substitution_fails_and_restores_target`
- `test_audit_context_first_candidates.ContextFirstCandidateAuditTests.test_unreadable_theoretical_subtree_is_rejected`
- `test_audit_context_first_candidates.ContextFirstCandidateAuditTests.test_unstatable_protected_root_is_not_treated_as_absent`

The first two have assertion-message mismatches; the third does not raise the
expected audit exception in this environment. No unrelated test or protection code
was changed, skipped, or softened. The task's acceptance uses its 32 passing scoped
tests, deterministic production output, pinned-source integrity, and bounded diff,
with these pre-existing full-suite failures explicitly retained.

## Production outcome

- 1,232 soldier records minus 367 untouched vanilla entries = 865 ROT candidates.
- 1,151 alternative rosters and 3,345 weapon-slot rows inspected.
- 1,719 melee slots plus 5 unresolved-item slots = 1,724 audited rows.
- 1,717 crafted-unvalidated + 2 ambiguous-slot + 5 unresolved = 1,724.
- Zero verified usage-specific damage/speed pairs; no numerical rank generated.
- 189 item-level evidence requests; 20 legacy sensitivity rows kept outside the
  current comparison; the legacy exact-profile template has zero data rows.

Missing values are not zero, a poor performance verdict, or permission to rank by
skills. Yi Ti remains the active empirical test because the operator explicitly
chose it, with its weapon-evidence status still pending. No replacement future
target is approved. Every unrelated queue closure, parked troop and historical
hold is preserved.

## Reproduction and rollback

Run the commands in `REPORT.md`. The final read-only workflow additionally compares
regenerated bytes against the committed outputs and verifies the canonical queue
matches `operator_queue_projection.json`. The JSON hash manifest covers the generator
outputs only; this narrative, the plan and diagnostic source inventory are separate.

To roll back, revert the PR squash commit after checking for newer operator queue
decisions. No source, empirical or frozen model data is modified by this task.
