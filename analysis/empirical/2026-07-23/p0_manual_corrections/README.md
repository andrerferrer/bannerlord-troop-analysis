# P0 manual corrections

This directory contains 50 image-backed review records split into small CSV parts. Each part repeats the header. Concatenate data rows after the first header, or pass every part to `apply_review_corrections.py`.

- corrected rows: 28
- confirmed rows: 22
- source: exact-hash 60-screenshot ZIP

Integrity hashes are recorded in `MANIFEST.json`.
