# Phase 2 review decisions

The 12 queued rows expand to 13 field-level decisions, and all remain unresolved. Raw screenshots are not retained for visual re-review, so the reviewed layer preserves null values. The analysis does not infer numeric counts. Every queued row remains outside the ordinary-troop primary input, so this is a documented limitation rather than a merge blocker.
