# Reviewed layer

The extension contains zero uncertain numeric rows, so `review_decisions.csv` is header-only. Six identity statuses are corrected conservatively against the committed Realm of Thrones troop audit; no normalized numeric value is changed.
