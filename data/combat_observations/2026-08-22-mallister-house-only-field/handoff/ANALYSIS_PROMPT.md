# Phase 2 handoff — Mallister house-only field cohort

Verify the Phase 1 hashes and treat every normalized file as immutable.

Analyze all visible player-side ordinary troops, not only the focus pair. Keep all four battles in `field`. Publish:

1. offensive efficiency `kills / deployed`;
2. retention `survivors / deployed`, with frontline defensive rank only for infantry-role rows;
3. one final side-level pressure margin per battle:
   `player_remaining / player_deployed - opponent_remaining / opponent_deployed`;
4. reliable versus insufficient partitions under the 5-battle / 20-deployed gate;
5. a focused Mallister House Guard and Mallister Eagle Knight comparison;
6. a protocol-separated comparison with the prior Ravens-present batch
   `2026-08-22-mallister-eagle-knight-dedicated`.

Do not transfer Ravens' kills to another unit, silently pool the two protocols, or interpret pressure margin as causal credit for an individual troop. Record the 20:32 open-field Garrison of The Crag result as field with medium context confidence.
