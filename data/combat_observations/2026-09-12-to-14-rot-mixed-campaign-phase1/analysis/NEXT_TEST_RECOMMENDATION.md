# Next-test decision

No queue change. This mixed campaign batch has no approved primary test troop, so it cannot select a new target. `active_test` remains null, `ordered_queue` remains empty, and Arryn Winged Knight remains under historical verification hold.
