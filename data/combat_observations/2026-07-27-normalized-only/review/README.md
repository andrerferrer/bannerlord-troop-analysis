# Phase 2 review decisions

All five queued values remain unresolved. Each is a hero `upgrade_ready` field derived from a visual icon. Raw screenshots are not retained for visual re-review, so the reviewed layer preserves null values and does not infer numeric counts. This is a documented limitation rather than a merge blocker because heroes remain excluded from ordinary troop rankings.
