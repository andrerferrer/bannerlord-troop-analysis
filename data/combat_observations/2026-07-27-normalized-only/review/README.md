# Phase 2 review decisions

All five queued values remain unresolved. Each is a hero `upgrade_ready` field derived from a visual icon. The original screenshots are not repository-addressable, so the reviewed layer preserves null values and does not infer numeric counts. Heroes remain excluded from ordinary troop rankings.
