# Phase 1 review queue

`review_queue.csv` preserves every unreadable or non-numeric field without guessing. Phase 2 must record decisions additively under this directory and must not rewrite normalized source rows.
