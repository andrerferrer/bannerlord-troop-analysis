# Source retention

Raw PNGs are not duplicated in Git. `source_inventory.csv` and `source_provenance.json` preserve exact per-image hashes, sizes, capture times, original ZIP provenance, and Library/file citation identifiers. The deterministic normalized archive is the repository-addressable Phase 2 input.
